import { Module } from '@nestjs/common';

// Placeholder module reserved for future user management endpoints.
// Auth/me already covers current-user needs in Phase 1.
@Module({})
export class UsersModule {}
